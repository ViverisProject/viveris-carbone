"""Authentication controllers (business logic scaffold)."""

from typing import Optional


def authenticate_user(username: str, password: str) -> Optional[str]:
    """Authenticate and return token (to be implemented)."""
    return None
